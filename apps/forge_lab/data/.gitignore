# Datos de runtime de Forge Lab. Los sets JSON se versionan (activos de reproducibilidad); la BD no.
*.sqlite
*.sqlite-journal
# Imágenes y manifiestos de regeneraciones (pesados, reproducibles desde el set)
validation_runs/
# Imágenes temporales de exploración por bloques (efímeras por diseño)
explore_tmp/
# Preferencia local de paths de modelos (como hub_config: no se versiona)
model_config.json
